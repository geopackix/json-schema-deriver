{
  "name": "json-schema-deriver",
  "version": "1.0.0",
  "description": "",
  "main": "./build/app.js",
  "scripts": {
    "build": "tsc --build",
    "start": "tsc --build && node ./build/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Manuel Hart",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.14.25",
    "typescript": "^4.1.3"
  }
}
