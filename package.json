{
  "name": "json-schema-deriver",
  "version": "1.0.0",
  "description": "",
  "main": "./build/app.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/geokoord/json-schema-deriver"
  },
  "scripts": {
    "build": "tsc --build",
    "start": "tsc --build && node ./build//example//createSchemaFromJSON.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Geopackix",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.14.25",
    "typescript": "^4.1.3"
  }
}
